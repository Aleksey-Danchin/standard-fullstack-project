entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
  ping:
    address: ":8082"

providers:
  docker:
    exposedByDefault: false
    network: xxyyzz-test-net
  file:
    directory: /etc/traefik/dynamic
    watch: true

api:
  dashboard: false

ping:
  entryPoint: ping

log:
  level: WARN
