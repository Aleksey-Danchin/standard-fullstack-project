# @scaffold-config — scaffold infra/scripts/env. Change project name, domains, and env only. See SCAFFOLD.md

# --- Postgres ---
POSTGRES_DB=xxyyzz
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# --- Backend ---
BACKEND_PORT=3000
SESSION_COOKIE_DOMAIN=xxyyzz.localhost

# --- Redis ---
REDIS_URL=redis://redis:6379

# --- Session ---
SESSION_ACCESS_TTL=10m
SESSION_REFRESH_TTL=7d
SESSION_TOKEN_SECRET=change-me-in-production
SESSION_MAX_PER_USER=0
SESSION_EVICTION_POLICY=oldest
REUSE_DETECTION_MODE=reject

# Публичный URL фронтенда в dev-режиме (локальный домен + HTTPS)
CLIENT_URL=https://xxyyzz.localhost
RECOMMENDED_CLIENT_URL=https://xxyyzz.localhost

# Боевой адрес приложения (production)
# PRODUCTION_URL=https://example.com

# --- Frontend (Vite) ---
VITE_API_BASE_URL=/api
VITE_API_CONCURRENCY=5
